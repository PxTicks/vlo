/**
 * The host menu catalogue is shell infrastructure (extension-shell-surfaces
 * plan §3.10, declarations in `core/shell/hostMenus.ts`); this module remains
 * as a re-export for existing imports.
 */
export {
  HOST_MENU_IDS,
  type HostMenuId,
} from "../../../core/shell/hostMenus";
