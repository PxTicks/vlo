# Trusted filter-pack fixture

Filter packs are ordinary frontend extensions that register one or more
`trusted-filter` transformations. They use the existing filter lifecycle and
therefore work in live preview and export without a second loader or registry.
